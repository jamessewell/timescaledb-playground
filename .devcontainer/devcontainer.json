{
  "name": "timescaledb-tutorial",
  "image": "timescale/timescaledb-ha:pg16",
  "postCreateCommand": {
	"nohup bash -c 'pg_ctl init && pg_ctl start -l logfile &'",
	"echo CREATE EXTENSION IF NOT EXISTS timescaledb > $HOME/.psqlrc",
	"echo CREATE EXTENSION IF NOT EXISTS timescaledb_toolkit >> $HOME/.psqlrc"
  },
  "postAttachCommand": "psql",
    "customizations": {
        "codespaces": {
            "openFiles": [
                "./sql/welcome.sql"
            ]
        }
    }
}
